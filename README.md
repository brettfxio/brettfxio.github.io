# My Jekyll Site

This is a clean Jekyll site based on the Poole template, but with branding removed and modernized features.

## Getting Started

1. Install Jekyll and dependencies: `bundle install`
2. Run the site locally: `bundle exec jekyll serve`
3. Open `http://localhost:4000` in your browser.
